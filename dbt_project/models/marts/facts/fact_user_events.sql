select
    event_id,
    resolved_user_id,
    device_id,
    user_id,
    advertiser_id,
    product_id,
    campaign_id,
    ad_group_id,
    creative_id,
    objective,
    channel,
    event_type,
    funnel_step,
    event_timestamp,
    event_date,
    session_id,
    audience_segment_id,
    targeting_tag_ids,
    country,
    region,
    city,
    device_type,
    platform,
    is_touchpoint,
    is_conversion,
    event_value_canonical,
    revenue,
    currency,
    kafka_topic,
    kafka_partition,
    kafka_offset,
    ingested_at
from {{ ref('int_identity_resolved') }}
